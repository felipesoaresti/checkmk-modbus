__pycache__/
*.pyc

# Only the current package build is tracked in git (see README "Repository
# layout"); older/previous .mkp builds are kept locally but not versioned.
modbus-*.mkp
!modbus-1.0.7.mkp
.venv
.venv/*
