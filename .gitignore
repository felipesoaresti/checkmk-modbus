__pycache__/
*.pyc

# Only the current package build is tracked in git (see README "Repository
# layout"); older/previous .mkp builds are kept locally but not versioned.
modbus-*.mkp
!modbus-1.0.11.mkp
.venv
.venv/*

# Local copies of third-party .deb/.rpm packages (libmodbus), kept for offline install
# on servers without repository access. Not part of this project - not versioned.
libmodbus-debs/
